# 公益智助柜系统上下文

本文档记录本仓库的核心业务语言。后续写代码、测试、问题单、PRD、架构建议时，优先使用这里的词汇。

## 系统目标

公益智助柜系统用于支持社区公益物资的投放、领取、柜机管理、库存追踪和运营分析。当前阶段优先跑通核心业务闭环，再逐步替换为正式数据层和更完整的数据分析能力。

## 用户角色

- 服务提供商：平台级运营方，只在全局范围创建和选择客户实例；进入实例后使用由服务端签发的新会话处理该实例业务，退出时再次轮换会话。
- 实例管理员：客户实例的首管理员或后续管理员，负责所属实例内的人员、后台账号、柜机、货品、仓库、规则、日志、系统配置和验证授权；界面中的“管理员”均指实例管理员，不另设同义角色。
- 商户：爱心商户或供货方，负责货品投放、补货、模板维护和领取去向查看。
- 补货员：由实例管理员建立并分配柜机，只能查看和操作明确分配给自己的柜机。
- 特殊群体：公益物资领取对象，按规则和时段领取物资。
- 普通用户：移动端展示层对特殊群体的友好称呼；代码中的领域角色仍以 `special` 为准。

## 核心对象

- 服务商平台：服务提供商用于创建、选择和维护客户实例的正式平台身份，不随某个实例或部署进程的数据平面变化。
- 客户实例：一个客户或运营单位的独立业务范围，拥有自己的人员、后台账号、柜机、货品、规则和审计边界。
- 实例服务类型：客户实例创建时确定的服务属性，分为正式服务和模拟服务；它不同于实例当前是试运行、运行中还是已暂停。
- 运行数据平面：部署进程承载业务数据和外部集成的隔离边界，分为模拟与真实；它是技术运行边界，不是服务商账号的身份标签。
- 柜机：公益物资自助柜，对应一个设备编号，可包含多个柜门和多个货品。
- 柜门：柜机中的可开门单元，开柜、门状态、结算回调都围绕柜门发生。
- 货品：可投放或领取的物资种类，例如三明治、牛奶、牙刷。
- 货品分类树：以“任意”为唯一根节点的最多八层树状目录，例如“任意 → 饮食 → 食品/饮料”和“任意 → 日用品”；每个货品只直接归属一个节点，节点后续新增的子类自动继承上级领取资格。
- 领取额度池：个人或策略在某个时段内对分类节点或指定货品授予的独立数量。多个额度池累加；实际领取时优先消耗最具体的额度池，再消耗祖先额度池，分配结果不依赖商品提交顺序。
- 货品批次：一次投放或库存调整形成的库存来源，包含数量、剩余数量、保质期、来源人等信息。
- 负库存平衡：当柜机平台回调显示实际扣减数量超过系统当前批次数量时，系统创建的正式库存平衡记录，用于保留真实业务流水并等待后续补货、盘点或人工处理修正。
- 仓库：柜机之外的本地库存位置，用于中转、调拨和盘点。
- 库存移动：领取、投放、过期、调整、补货、扣减、退款等引起的库存流水。
- 消费追踪：记录某次领取、扣减或调整消耗了哪些货品批次，以及批次来源人与消费人的对应关系。
- 人工结算补记：柜门可信关闭满 10 分钟仍缺少平台结算明细时，由实例管理员绑定原开柜事件、按现场盘点补记实际领取的受控流程；它同时更新库存、额度、事件、任务和审计，并可继续完成平台回写。
- 库存纠错：仅用于修正本地库存与人员记录的手工补货或补扣，不创建或补写开柜结算，也不在柜机平台创建订单。
- 平台补扣回调：SmartVM 对已经存在的平台订单推送的差额调整事实；它来自可信平台回调，不等同于后台发起的人工结算补记。

## 核心流程

- 实例开通：服务提供商选择正式服务或模拟服务后，一次创建客户实例、首管理员人员记录和后台凭据；任一步失败时整体回滚。正式服务只有在独立真实数据平面通过生产门禁后才算正式运行。
- 实例切换：服务提供商进入或退出客户实例时轮换后台会话，实例身份和 `tenantId` 只由服务端会话决定，不接受客户端自报。
- 账号授权：平台初始化根信任建立首个服务提供商；服务提供商创建实例及首管理员；实例管理员只能在所属实例内管理同级后续管理员和下级角色。任何角色都不能向上创建、重置或接管服务提供商账号，每个实例始终保留至少一名启用、具有本实例有效后台凭据且可登录的实例管理员。
- 手册可见性：服务提供商可查看全部角色手册；实例管理员、商户和补货员只能查看本身份及其下级角色手册，任何实例角色都不能看到上级角色的操作入口或维护方式。
- 密码恢复：服务提供商知道当前密码时在平台 UI 自行改密；遗忘密码时只能由平台初始化根信任在 Spark VNC 本机通过固定目标、停写、备份、单写租约和审计门禁紧急恢复，实例找回页和任何租户角色都不能重置服务商账号。实例管理员可使用后台账号、绑定手机号和 `password-reset` 专用验证码自行重置密码；无法自助时，由服务提供商进入目标实例后填写原因代重置。通用账号权限配置不承担已有账号改密。
- 柜机分配：实例管理员给商户或补货员分配柜机；列表、详情和开柜预检使用同一份授权结果。
- 人工验证码：实例管理员可为当前实例中已存在且启用的账号签发 6 位短期验证码，用于登录或密码重置；验证码有用途、有效期、失败次数、撤销和单次消费边界。
- 注册审核：移动端按受信代理还原的已登记实例域名提交身份和资料，不接受客户端自报实例；实例管理员只能查看和审核所属实例申请，审批创建的账号和登录会话继承该实例归属。
- 取货规则：特殊群体按业务日、时段和领取额度池领取货品；预约、开柜预结算、平台结算和退款使用同一套确定性分配规则。
- 业务日：系统统计领取完成度和额度时使用的业务日期，不一定等同自然日。
- 开柜：用户或管理员向柜机平台发起开门请求，系统创建柜机事件并等待平台回调。
- 预结算：开柜前根据意向货品和免费额度校验本次是否都在可领取范围；当前阶段一旦超出范围即阻止开柜，不进入支付。
- 结算回调：柜机平台回传实际取走或投放的货品，系统更新事件、库存、金额和日志。
- 缺失结算恢复：可信关门满 10 分钟仍无结算时生成待处理任务；实例管理员按现场实际商品完成本地补记，再复用同一交易号完成或重试平台回写。迟到回调按事件互斥核对，不重复扣减。
- 领取差异：实际结算与预期不一致时，系统记录差异并交由管理员核对；当前公益领取不向用户发起补扣或支付。
- 库存批次变化：统一处理投放、领取扣减、手工补货、手工扣减、调拨、盘点修正和撤销中的批次、库存移动、消费追踪与柜机库存同步。
- 预约：特殊群体提前锁定柜机货品，到期未履约会计入超时。
- 货品分类迁移：先生成只读预览和哈希，再在数据未漂移时一次应用；迁移保留货品、分类、库存与历史流水，清空旧额度规则并自动取消受影响的有效预约。
- 预警：系统或用户反馈产生的待处理任务，例如临期、低库存、柜机故障、服务问题。
- 操作日志：面向运营追踪和撤销的业务动作记录，不只是系统审计日志。

## 外部系统

- SmartVM：柜机平台，负责开门、货品查询、门状态、结算、补扣和支付相关回调。
- 短信供应商：用于发送验证码；真实短信当前使用阿里云 PNVS 的 Send/Check 契约。
- 支付供应商：仅保留用于历史支付记录的查询、退款和恢复，不进入当前公益领取用户流程。
- 大模型供应商：用于 AI 运营助手、异常诊断、日报和策略建议。
- 高德地图：用于后台或移动端点位选择和地理信息展示。

## 应用与包

- `apps/api`：NestJS 后端，承载主要业务逻辑。
- `apps/mobile`：uni-app + Vue 3 移动端，面向特殊群体、商户和部分管理场景。
- `apps/admin-web`：Vue 3 后台，面向管理员和商家后台。
- `packages/shared-types`：共享领域类型、接口类型与演示种子数据。
- `packages/shared-client`：通用 HTTP 请求层与 SmartVM 签名工具。
- `sandbox`：接口联调和回调模拟脚本。

## 当前约束

- 首版后端使用内存种子数据和本地持久化文件，目标是先稳定业务流程和接口形状。
- 产品运行仍只有模拟与实机两种数据平面；mock、阿里云 PNVS 和后台签发人工码是验证实现方式，不是新的运营模式。
- 服务商平台不得因当前进程运行在模拟数据平面而显示为“模拟服务”；实例服务类型、实例生命周期和运行数据平面必须分别展示和校验。
- 非默认实例只开放已完成租户边界校验的后台路由；尚未租户化的旧业务路由保持关闭式拒绝。
- 客户实例公开域名必须唯一；未知域名、重复域名、暂停实例以及无法归属柜机或事件的回调摘要均关闭式拒绝。
- SmartVM 货品查询属于只读观测，不得借只读权限更新全局货品目录或柜机本地货品配置。
- 人工结算补记必须绑定所属实例内的原开柜事件和可信关门记录；重复提交、跨实例订单号和已经存在结算流水的事件均关闭式拒绝。
- 人工验证码必须绑定签发人、实例、目标账号、用途、有效期和单次消费状态，持久化时不得保存明文验证码或手机号。
- 商户和补货员的权限上限由角色固定，实例管理员不能把人员管理、后台账号管理、系统设置等管理员权限下发给商户或补货员。
- 当前开放给特殊群体的公益物资全部免费；超出可领取范围时阻止开柜，异常金额只进入管理员核对。
- 分类树只能有一个根节点且最多八层；分类移动或停用必须先预览影响范围并提交匹配的 revision，受影响预约自动取消并留审计记录。
- 额度池分配必须可重放且与请求顺序无关；新流水保存精确额度池分配，旧流水按实际货品在当前规则中兼容映射。
- SmartVM 敏感凭据只从环境变量读取，不写入代码或演示数据。
- 前端界面文案应集中维护，避免业务逻辑散落在页面文案中。
- 仓库文档、代码注释和 issue/PRD 默认使用中文，除非任务明确要求其他语言。
