# 待办单击处理与回调警告排查

2026-09-23。沿用 `codex/admin-workspace-redesign` 分支；本次排查生产数据只读，没有补写领取、修改库存、重发回调或操作真实柜门。

## 待办交互修改

工作台、待办列表和柜机详情中的“标记已知晓”“手动完成”现在单击直接提交，不再出现浏览器确认弹窗。保留权限检查、处理中提示、成功/失败反馈；提交期间统一禁用处理按钮并拒绝重复进入处理函数。

这两个动作仍只处理提醒状态；“手动完成”不会补记结算或修正库存。故障的“已知晓”也不表示设备故障已经排除。开柜、退款、库存处置等独立业务确认未改动。

本地 Node 22.22.2 / npm 10.9.7 执行 `check:local:full`：1016 项，1010 通过、0 失败、6 项 Windows 平台跳过，各端构建完成。新增 9 项组件流程覆盖三个入口的单击处理、快速连点、失败重试；隔离模拟数据校验通过。浏览器实际完成一条模拟预警、知晓一条模拟故障；桌面及 375px 宽度均未出现二次弹窗，console error 为 0。截图留在 `.codex-run/todo-callback-20260923/`。

## 回调警告的含义和已查证原因

后台“结算回调超时待补记”的触发条件是：特殊群体开柜事件已有可信关门记录，超过 10 分钟仍没有对应领取/投放结算流水，也没有有效人工补记。这不是后台改版新引入的提示。

最近三笔代表性事件如下，时间均为北京时间 UTC+8：

| 事件 | 关门 / 首次回调 | 结算入账与平台回写 | 查证结果 |
| --- | --- | --- | --- |
| `event-mudm5i5c3q0bm` | 9 月 23 日 12:39:40 关门 | 14:45:16 入账，约 126 分钟后；平台回写成功 | 结算请求直到 14:45:16 才抵达 API，接收后约 0.35 秒完成处理 |
| `event-mudmo52o28t41` | 9 月 23 日 12:54:11 关门 | 14:36:31 入账，约 102 分钟后；平台回写成功 | 结算请求直到 14:36:31 才抵达 API，接收后约 0.28 秒完成处理 |
| `event-mucla7747pylh` | 9 月 22 日 19:27:42 关门，19:28:56 已收到一次回调 | 9 月 23 日 14:58:51 重试后入账，平台回写成功 | 首次处理报“请求货品 8243857 不存在”；9 月 22 日 23:00:29 同步新增该货品“面包新语”，下一次回调重试才完成结算 |

前两笔的延迟发生在请求抵达当前 API 之前。本方日志不足以进一步区分平台识别耗时、平台投递/重试与上游链路延迟，不能直接断言供应商某个内部环节故障。第三笔则有明确的本地货品目录缺项证据；新增平台货品投入使用前应先完成“同步平台货品”，避免等到夜间自动同步才补齐目录。

第三笔首次处理的审计行虽然带有默认 `statusCode=200`，实际记录为 `auditPhase=indeterminate`，平台方向日志有上述异常且 `actualResponseBodyRecorded=false`。因此不能把该行的 200 当作成功处理；原始回调摘要的 `receivedAt` 也只是首次收到时间，重试复用摘要后不会变成真正入账时间。

## 当前影响与提醒为何仍显示

排查时三笔事件均为 `settled`，各有且仅有一条领取流水（数量 1）、一条额度分配，`paymentNotifyStatus=success`。没有对用户发起支付；“平台完成回写”是零元公益订单的状态确认。全量检查当前 live 事件，未发现特殊群体“已关门但没有领取/投放结算流水”的记录。

所以当前领取账务已经恢复，但等待期间库存、额度和运营统计曾延迟更新，不能把警告理解为始终无影响。上述入账都早于此次后台重构第一次停服时间 14:59:38，时间线不支持将这些问题归因于后台改版。

当前系统自动关闭的是已恢复的“平台完成回写失败”等指定提醒；“结算回调超时待补记”在收到正常回调后仍保留人工处理状态。三笔提醒在 15:18 由管理员手动完成，业务入账时间均更早。历史日志也会继续保留警告作为追溯记录。本轮没有降低超时阈值、屏蔽回调警告或通过关闭提醒代替业务结算。

## 定位入口

- 待办按钮：`apps/admin-web/src/pages/DashboardPage.vue`、`AlertsPage.vue`、`DeviceDetailPage.vue`。
- 组件回归：`apps/admin-web/src/tests/workspace-flows.test.ts` 的“待办直接处理”。
- 超时提醒规则：`apps/api/src/modules/alerts/alerts.service.ts` 的 `refreshManualSettlementTasks()`。
- 正常结算与重试：`apps/api/src/modules/cabinet-events/cabinet-events.service.ts` 的 `handleSettlement()`。
- 生产证据：受控 live 平面的 `store.json` 中事件/流水/额度/告警/同步日志，以及 `system-audit.ndjson` 的回调请求和异常记录；不将原始账本或凭据提交到 Git。

本文件提交时本地验证已完成；正式发布结果与新回滚点另记于后续发布回执。
