# 2026-09-21 微信审核整改：先浏览、后登录

## 变更与范围

原版本的启动页、底部栏目及柜机详情在没有会话时跳转登录，首次进入无法了解服务、浏览物资。现改为游客可浏览服务介绍、柜机位置、物资和可用库存；点击个人领取权益、记录或账户的登录按钮后才进入登录流程。

- 首次进入、已退出登录、登录凭证失效或资格资料尚未完成时，均允许浏览公开服务。
- 记录、我的展示可取消的自主登录入口；登录、资料填写和审核状态页均有继续浏览入口。
- 柜机查询入口登录后仍为查询模式，现场扫码来源则保留原柜机目标；取消登录清除待办目标，防止下次登录误跳转。
- 新增 `GET /api/public/devices`、`GET /api/public/devices/:deviceCode`，按受信请求域名限定实例，只返回公开字段，禁止缓存。
- 游客查询使用原有可用库存计算，保留商品停用、负库存、预约占用和保质期策略。查询不会同步映翰通、生成订单、开门或改写账本。
- 原领取资格、实际领取才消耗权益、开门认证和私人数据接口未放宽。

## 本地验证

工作分支：`codex/guest-browse-wechat-review-20260921`，基于 `45848d5`。

运行环境：

- API：`http://127.0.0.1:4057/api`。
- H5：`http://127.0.0.1:5187/`，`VITE_API_BASE_URL=http://127.0.0.1:4057/api`。
- 后台页面本轮未启动；后台类型检查、57 项测试与构建已由完整门禁覆盖。
- 独立临时模拟数据，`VM_TEST_ISOLATED_ENV=1`，支付与验证码均为 mock。模拟账号未触及正式审核账号或其一次性验证码。

自动检查：

- `npm run check:local:full`：退出码 0。包含类型检查、API、移动端、后台及运维测试、库存/数据完整性/前端安全冒烟，以及 API、后台、H5、微信、支付宝和 App 构建。
- API：683 项，680 通过、3 项按现有条件跳过、0 失败。
- 移动端：94 项全部通过；后台：57 项全部通过。
- 新增游客接口测试覆盖真实批次、货号别名、停用、负库存、已预约数量、过期仅提醒与强制过滤两种模式、公开字段白名单、零业务副作用。
- 真实本地 HTTP 测试覆盖域名实例隔离、忽略客户端 tenantId、未知/重名/暂停入口、跨实例柜机、私人接口与开门拒绝匿名调用。
- 微信构建 AppID 为 `wx4f14e307e19e68c1`，生产 API 固定为 `https://vending.5gogogo.top/api`，构建大小 1463.69 KiB。
- `npm run verify:api-data`：本地模拟账本通过。模拟种子账号的默认密码告警仅属于隔离测试数据，该数据不发布。
- 只读获取的正式 store 文件副本以本版校验器验证通过，0 错误，3 条既有负库存平衡批次告警。此项仅验证 store 副本；正式审计日志未同步，在线读取副本也不能作为发布回滚备份。

浏览器实际点验（390 × 844）：

1. 首次打开进入游客首页，可看到服务介绍、两台模拟柜机及真实模拟账本库存，无登录弹窗。
2. 首页 → 附近柜机 → 柜机详情，可查看物资库存；记录、我的保持自主登录提示，不自动跳转。
3. 从柜机详情点击登录，再选择暂不登录，返回原柜机库存页。
4. 用隔离模拟账号完成登录，回到原查询柜机，库存及本人权益加载成功，仍须现场扫码领取。
5. 退出登录返回游客首页，公开库存仍可浏览。
6. 从扫码路由进入也可先浏览；点击登录保留扫码来源，取消登录返回相同柜机。
7. 手机宽度无横向溢出，刷新按钮与标题正常排版，浏览器错误日志为空。

未覆盖真实短信、实际开门、实际领取、映翰通回调、微信原生运行及微信审核结果。上述扫码点验为本地扫码入口路由，不代表扫描了真实柜机二维码。

## 发布状态与顺序

本次未部署正式服务、未重新上传微信开发版、未提交审核。只读检查时正式 Git 为 `c31db1d`，API 与 Web 服务均 active。

正式后端必须先按项目发布流程更新，验证公共接口返回正确实例的库存后，再上传新小程序包；仅上传前端会因缺少公共接口而无法显示游客库存。

此前停止、更新和重启正式服务的命令被自动审批以 `blocked by policy` 拒绝。本次不重试或更换执行渠道完成该被拒绝操作。微信审核浏览器页面亦曾被站点安全策略阻止，本次未尝试绕过。

发布时还须按项目流程取得停写窗口及单写者租约，备份并校验正式数据；本记录的在线只读副本不替代该备份。完成正式后端部署、微信开发者工具上传及新版本实际浏览验收后，才可以将下列说明用于重新提交审核。

## 重新审核说明草稿

> 已整改首次进入强制登录的问题。打开小程序可直接浏览公益服务介绍、柜机位置、物资和库存，无需授权手机号、头像或昵称。仅当用户主动选择查看个人领取权益、领取记录或办理领取资格时，才进入登录流程；登录页可选择“暂不登录，继续浏览”。实际开门领取仍需核验资格。请从首页及“附近柜机”体验未登录浏览；个人功能可使用单独提供的审核测试账号。

这段说明只描述本版实现，不能据此宣称已经上线或保证审核通过。
