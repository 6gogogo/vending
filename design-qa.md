# 移动端设计验收

## 统一登录流程

**对比目标**

- 设计真值：`C:\Users\LEGION\.codex\visualizations\2026\08\08\019fe03e-85d0-7a00-ae0d-b3f1e3566ea8\unified-auth-flow-prototype\qa\source-selected.png`
- 实现截图：`qa/unified-auth-login-implementation-final.jpg`
- 同屏对比：`qa/unified-auth-login-comparison-final.png`
- 补充状态：`qa/unified-auth-new-profile-empty.jpg`、`qa/unified-auth-prefill-profile-final.jpg`、`qa/unified-auth-review-pending-final.jpg`
- 视口：393 × 852 CSS px，devicePixelRatio 1.5；实现截图为 393 × 852 px。
- 设计源：853 × 1844 px；比较时高质量缩放到 393 × 852 px，与实现截图统一密度和裁切。
- 状态：统一登录/注册空表单；补充验收新用户空资料、已导入资料锁定、审核中。

**检查结果**

- 无 P0、P1、P2 遗留问题。字体层级、表单可读性、间距节奏、绿色/暖白色视觉令牌、背景插画及业务文案均与确认稿保持同一设计语言；393 px 视口无横向溢出。
- 可接受差异：真实 uni-app H5 使用系统字体与平台原生表单渲染，边缘抗锯齿和参考栅格稿存在轻微差别；不影响层级或交互。
- P3：参考稿含输入框图标，现有实现为更克制的纯输入框。因当前小程序未引入匹配图标库，且不使用文字符号或手绘替代，保留为后续非阻断润色项。

**五项保真检查**

- 字体与排版：主标题、品牌标题、卡片标题、字段标签、按钮和辅助操作层级清楚，字号放大后未错位或截断。
- 间距与布局：页面、品牌区、表单卡、输入框和按钮对齐稳定；正文宽度 393 px，`scrollWidth` 与视口一致。
- 色彩与令牌：暖白背景、公益绿、浅绿提示和少量橙色点缀与设计源一致，对比度可读。
- 图片质量：直接复用已确认的 `vm-auth-hero.png`，裁切清晰，无占位资产、文字图标或代码绘图替代。
- 文案与内容：登录/注册合并、一次验证码、资料核对、审核中及工作人员入口符合锁定流程；设计自述未进入页面。

**交互与状态验收**

- 新手机号完成一次验证码后进入资料页；姓名为空，身份两项均未选中，片区显示“请选择所在片区”。
- 后台导入手机号进入预填资料核对；身份和所在片区只读，不渲染身份切换或片区选择器；确认后直接进入服务首页。
- 新用户只提交一次注册资料即进入审核页；刷新页面仍停留审核页；API 服务重启后点击刷新仍保持审核中。
- 扫码目标的保存与审核/登录续接由移动端自动测试覆盖；确认用户路径的浏览器验收未打开或调用真实柜机。
- 页面控制台无应用错误；仅有 uni-app 依赖中的 `vue-router` 旧导入路径弃用警告，非本次代码产生且不影响运行。

**比较历史**

- 第一轮证据：`qa/unified-auth-login-comparison-v1.png`。发现品牌区高度、表单卡密度和背景角落装饰与确认稿存在 P2 偏差。
- 修正：调整品牌区高度、卡片内边距与段落间距、免责声明字号及页面背景装饰；对应提交 `532b94a`。
- 复验：`qa/unified-auth-login-comparison-final.png`。上述 P2 均已消除；未发现新的 P0/P1/P2。

**实施检查清单**

- [x] 同视口、同状态、同密度比较设计源与真实页面。
- [x] 验收新用户无默认身份/片区。
- [x] 验收导入资料的身份/片区不可修改。
- [x] 验收审核态刷新与服务重启恢复。
- [x] 检查页面溢出、交互和控制台。

最终结果：通过

---

## 附近柜机并列入口单行修复

### 对照基准

- 设计真值：`docs/test-artifacts/nearby-choice-nowrap-20260816/source-wrapped-actions.png`，592 × 1286 px；原图约为 390px 手机内容的高密度截图。
- 演示实现：`docs/test-artifacts/nearby-choice-nowrap-20260816/implementation-demo-nowrap.png`，974 × 885 px，包含居中的 430px 方案 A 手机画布。
- 正式实现：`docs/test-artifacts/nearby-choice-nowrap-20260816/implementation-formal-nowrap.png`，390 × 844 px，CSS 视口 390 × 844，devicePixelRatio 1。
- 状态：特殊用户“附近柜机”首页，两个并列领取方式均可见。
- 比较方式：同一输入中并排检查原问题截图、修正后的方案 A 演示页和正式 H5 页面；另在 320px 正式视口复验响应式边界。

### Findings

- 第一轮 P2：两个入口受可用宽度限制，四字尾部被挤到第二行，造成按钮高度和阅读节奏不整齐。
- 修正：演示页让入口行使用完整卡片宽度、响应式字号并置于装饰图形上方；正式页面为两个并列入口增加 `min-width: 0` 与 `white-space: nowrap`。
- 复验：390px 正式页面两个入口高度均为 34.31px，`scrollWidth === clientWidth === 155px`；320px 时高度均为 28.16px，`scrollWidth === clientWidth === 126px`，页面横向滚动宽度仍为 320px。
- 无遗留 P0、P1、P2；演示页和正式页面控制台均无 error。

### 五项保真检查

- 字体与排版：文案、字重和层级保持不变；两个入口均为单行，不截断、不溢出。
- 间距与布局：继续使用两列等宽结构；只释放演示页原本被右侧插画预留区占用的横向空间。
- 色彩与令牌：边框、暖白底色和公益绿色完全沿用现有令牌。
- 图片质量：柜机插画及背景装饰未替换、未拉伸；入口层级调整后装饰不再压淡文字。
- 文案与内容：仍为“提前预约 · 保留物资”和“到柜扫码 · 直接领取”，未改业务语义。

### 实施检查清单

- [x] 390px 演示页两个入口单行显示。
- [x] 390px 正式页面两个入口单行显示。
- [x] 320px 正式页面无换行、截断或横向溢出。
- [x] 移动端 85/85、类型检查和微信构建通过。
- [x] 正式 AppID 与公网 API 构建校验通过。

final result: passed

---

## 商品分类额度

### 对照基准

- 用户确认的交互原型：分类标题跟随滚动、两列大图商品卡、`专属 n/n · 任意 n/n`、商品数量使用“未预约”。
- 正式实现页面：`apps/mobile/src/pages/special/device-detail.vue`，390 × 844 H5 视口，匿名隔离数据平面。
- 正式实现截图由隔离验收夹具生成在 `.codex-tmp/taxonomy-mobile-qa/`；该目录是临时证据，不构成运行或构建依赖。

### 验收结果

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 分类层级 | 通过 | 任意物资 → 饮食 → 食品/饮料，以及独立日用品分组均按后台 `sortOrder` 与父子路径呈现。 |
| 额度表达 | 通过 | 分类标题显示 `专属 n/n · 任意 n/n`；任意额度只在根节点计一次，并随实际选择实时变化。 |
| 商品卡片 | 通过 | 两列大图并列，名称、未预约数量和大号加减按钮可在 390px 宽度下完整显示。 |
| 额度上限 | 通过 | 专属额度优先分配，超出部分消耗任意额度；全部可用额度耗尽后增加操作不再生效。 |
| 吸顶交互 | 通过 | H5 实际滚动容器中，当前分类标题固定在导航栏下方，下一分类标题到达时依次顶替。 |
| 响应式 | 通过 | 页面与根节点横向溢出均为 0；未发现文字截断、卡片越界或横向滚动。 |
| 未预约数量 | 通过 | 特殊用户商品查询与预约创建共用可预约库存，已被其他有效预约锁定的数量不会重复展示。 |
| 兼容回退 | 通过 | 缺少分类版本或额度池时继续使用原线性商品列表，不把旧账号误判为零额度。 |
| 控制台 | 通过 | 正式页面无业务错误；仅有 Vue Router 既有弃用警告。 |

### 偏差说明

正式页面保留现有柜机名称、预约说明和提交按钮，不复制原型中的演示外壳；商品选择区的层级、额度语义、尺寸和交互与用户确认稿一致。

---

## 附近柜机方案 A

### 对照基准

- 设计真值：用户确认的附近柜机方案 A。
- 正式实现：`apps/mobile/src/pages/tabs/nearby.vue` 与预约凭条 `apps/mobile/src/pages/special/device-detail.vue`。
- 真实页面证据在隔离验收目录中逐页检查；临时截图不作为仓库构建依赖，也不在文档中引用不存在的路径。
- 视口：390 × 844 CSS px；API 使用隔离的本地模拟数据平面，未连接真实柜机、生产账号或支付链路。

### 验收结果

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 商品条目边界 | 通过 | 12 种商品的柜机卡片仅渲染 3 条预览，并显示“查看全部 12 种物资”；10,000 条输入的单测仍只返回 3 条预览。 |
| 完整商品列表 | 通过 | 底部弹层固定最大高度，内部滚动容器包含全部 12 条商品；页面主体不会随商品数量无限拉长。 |
| 两种领取方式 | 通过 | “提前预约 · 保留物资”和“到柜扫码 · 直接领取”并列展示，不再表达成前后步骤。 |
| 定位降级 | 通过 | 页面调用手机系统定位；未授权时显示“使用手机定位”，并继续按推荐顺序展示，预约不被阻断。 |
| 位置数据边界 | 通过 | 用户定位与柜机坐标分别处理；柜机无坐标时显示“柜机位置待设置”，不把手机定位失败误判为柜机资料缺失。 |
| 预约时效 | 通过 | 实际创建预约后，页内凭条同时显示“可用时段”“过期时间”和有效状态。 |
| 旧提示移除 | 通过 | 特殊用户页面不再渲染“找柜机提示”。 |
| 控制台 | 通过 | 真实 H5 页面无应用错误；未发现横向溢出、按钮遮挡或不可关闭弹层。 |

### 五项保真检查

- 字体与排版：柜机名、商品名、库存/可领取信息和主要操作层级清楚；三条预览的字号与确认稿一致。预约凭条四字标签保持单行。
- 间距与布局：卡片保持现有暖色页面栅格；预览、展开入口和主按钮间距稳定。完整列表采用受限高度弹层，不扩张柜机卡片。
- 色彩与令牌：继续使用项目现有公益绿、暖白背景、浅绿状态色与橙色细线，没有引入另一套视觉令牌。
- 图片质量：继续复用项目已有柜机插画和商品图片回退规则；未新增占位图、文字图标或低清资产。
- 文案与内容：预约与扫码两种方式并列；定位失败、柜机未配置位置、预约可用时段和过期时间分别使用独立文案。

### 比较历史

- 第一轮：方案 A 与真实 390 × 844 页面同屏比较，商品卡片、三条预览、展开入口和操作按钮层级一致。
- 第一轮发现 P2：预约凭条中的“可用时段”“过期时间”在窄屏被固定标签宽度挤成两行。
- 修正：仅在 420px 以下取消凭条标签固定宽度并禁止标签内换行；提交 `393033f`。
- 复验：五个凭条标签高度均为单行，12 条商品弹层可滚动且关闭按钮持续可见；无遗留 P0、P1、P2。

最终结果：通过

final result: passed
